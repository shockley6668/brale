package decision

import "brale/internal/types"

type FeatureReport = types.FeatureReport
type PositionSnapshot = types.PositionSnapshot
type ExitPlanSpec = types.ExitPlanSpec
type AccountSnapshot = types.AccountSnapshot
